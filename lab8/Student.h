#pragma once
#include <string>

using namespace std;

struct student
{
	string name = { 0 };
	string surname = { 0 };
	unsigned int age = 0;
	string faculty = { 0 };
	unsigned int level_of_grant = 0;
};